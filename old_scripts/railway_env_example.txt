# Railway PostgreSQL Database Configuration
# Copy this to .env file to use the new Railway database

# Database URL for Railway PostgreSQL
DATABASE_URL=postgresql+asyncpg://postgres:wHGvcsZIyxRQaJmpkqRfFnwheFUlZfUz@maglev.proxy.rlwy.net:26951/railway

# Alternative sync URL (if needed)
DATABASE_URL_SYNC=postgresql://postgres:wHGvcsZIyxRQaJmpkqRfFnwheFUlZfUz@maglev.proxy.rlwy.net:26951/railway

# Application settings
APP_NAME=Tamankehati
APP_VERSION=1.0.0
DEBUG=false
ENVIRONMENT=production

# Security settings
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS settings
CORS_ORIGINS=["https://your-frontend-domain.com"]

# File upload settings
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx

# Email settings (if using email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_TLS=true

# Redis settings (if using Redis for caching)
REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=INFO
